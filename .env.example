# Server
PORT=4000
NODE_ENV=development

# MongoDB (use Atlas connection string or local)
MONGODB_URI=mongodb://localhost:27017/aiinterviewbot

# Optional: for future auth (e.g. Firebase Admin or JWT)
# FIREBASE_PROJECT_ID=
# FIREBASE_CLIENT_EMAIL=
# FIREBASE_PRIVATE_KEY=
